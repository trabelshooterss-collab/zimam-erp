DEBUG=True

# ⚠️ SECURITY CRITICAL - غيّر هذا في الإنتاج!
# استخدم: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
DJANGO_SECRET_KEY=your_random_secret_key_here_CHANGE_IN_PRODUCTION

# Database
DATABASE_URL=postgresql://zimam_user:secure_password@localhost:5432/zimam_db
DB_NAME=zimam_db
DB_USER=zimam_user
DB_PASSWORD=secure_password
DB_HOST=localhost
DB_PORT=5432

# Cache & Message Queue
REDIS_URL=redis://localhost:6379/0

# AI Integration
# احصل على مفتاح من: https://makersuite.google.com/app/apikeys
GEMINI_API_KEY=<احصل_على_المفتاح_من_Google_AI_Studio>

# Email (اختياري)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_HOST_USER=your_email@gmail.com
EMAIL_HOST_PASSWORD=your_app_password

# Frontend URL (للـ CORS)
FRONTEND_URL=http://localhost:3000

# Allowed hosts (في الإنتاج: "yourdomain.com,api.yourdomain.com")
ALLOWED_HOSTS=localhost,127.0.0.1

# Sentry (اختياري - لتتبع الأخطاء)
# SENTRY_DSN=https://xxxxx@xxxxx.ingest.sentry.io/xxxxx

# Supabase (اختياري - إذا كنت تستخدمه من Backend)
# SUPABASE_URL=https://scwpavlyxhzlnibhlscv.supabase.co
# SUPABASE_SECRET_KEY=<السر_من_Supabase_Settings>

# Django settings
DEBUG=True
SECRET_KEY=your-secret-key-here
ALLOWED_HOSTS=localhost,127.0.0.1

# Database settings
DB_NAME=zimam_db
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432

# Email settings
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-password
DEFAULT_FROM_EMAIL=Zimam ERP <noreply@zimam.com>

# Frontend URL
FRONTEND_URL=http://localhost:3000

# Redis settings
REDIS_URL=redis://localhost:6379/0

# Google Gemini API Key
GOOGLE_API_KEY=your-google-gemini-api-key-here

# JWT settings
JWT_SECRET_KEY=your-jwt-secret-key-here
JWT_ACCESS_TOKEN_LIFETIME=60
JWT_REFRESH_TOKEN_LIFETIME=1440
